# TODO

##
unexpected end of input, Expected ':' after property namerustcClick for full compiler diagnostic
unexpected end of input, Expected ':' after property namerust-analyzermacro-error
extern crate core
The Rust Core Library
The Rust Core Library is the dependency-free[^free] foundation of The Rust Standard Library. It is the portable glue between the language and its libraries, defining the intrinsic and primitive building blocks of all Rust code. It links to no upstream libraries, no system libraries, and no libc.

maybe that is conflicting with our macro perhaps css oriented names, also make sure we are wrapping all values in double quotes

## enforce all css to be double quoted

## action testing 


# Done

- how to handle global?
  - we would also need a global tag for the style <style global>
- snake case for css?
- list-style should be list_style

# ? Much later 
- maybe improve css validation 
- make the lessons